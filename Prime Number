/// 0-n number print...
#include <bits/stdc++.h>
using namespace std;
#define Max 100
bool prime[Max];
vector<int> v;
void sieve(int n) {
  for ( int i = 0; i<Max; i++) prime[i] = true;
  for ( int i = 2; i*i<=Max ; i++) {
    if( prime[i]) {
      for ( int j = i*i; j<=Max; j+=i) {
        prime[j] = false;
      }
    }
  }
  for ( int i =2; i<Max; i++) {
    if(prime[i]) {
      v.push_back(i);
    }
  }
  for(int i = 0; i<v.size(); i++) {
    cout<<v[i]<<endl;
  }
}
int main() {
  int n;

  sieve(n);
  return 0;
}
